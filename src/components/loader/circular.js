import {
    CircularProgress,
    withStyles
} from '@material-ui/core';

const styles = {
    colorPrimary: {
        
    }
};

const Circular = withStyles(styles)(CircularProgress);

export default Circular;
