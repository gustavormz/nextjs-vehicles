import {
    Skeleton
} from '@material-ui/lab';

const SkeletonSimpleText = () => (
    <Skeleton animation="wave" height={30} width="100%" style={{ marginBottom: 6 }} />
);

export default SkeletonSimpleText;
