import {
    Skeleton
} from '@material-ui/lab';

const SkeletonCardTitle = () => (
    <Skeleton animation="wave" height={35} width="95%" style={{ marginBottom: 6 }} />
);

export default SkeletonCardTitle;
